import styled from 'styled-components';

const StyledArtList = styled.section`
	display: grid;
	grid-template-columns: 40% 40%;
	place-content: center;
	row-gap: 50px;
	padding: 2rem;
`;

export default StyledArtList;
